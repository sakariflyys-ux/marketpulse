import { ComingSoon } from "@/components/coming-soon";

export const metadata = { title: "Saved" };

export default function SavedPage() {
  return (
    <ComingSoon
      title="Saved"
      phase={4}
      summary="Nested folders of saved stores and ads with drag-and-drop organisation."
    />
  );
}
